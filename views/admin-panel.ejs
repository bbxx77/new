<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - User Management</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        .navbar {
            background-color: #333;
            color: white;
            padding: 10px;
            width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .navbar-links {
            display: flex;
            align-items: center;
        }
        
        .navbar-link {
            margin-right: 10px;
            color: white;
            text-decoration: none;
            padding: 8px;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .navbar-link:hover {
            background-color: #555;
        }

        h1 {
            color: #333;
            margin-top: 20px;
        }

        table {
            width: 80%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #333;
            color: white;
        }

        td {
            background-color: #f9f9f9;
        }

        a {
            text-decoration: none;
            color: #333;
        }

        form {
            margin: 0;
        }

        button {
            padding: 8px;
            font-size: 14px;
            cursor: pointer;
            background-color: #d9534f;
            color: white;
            border: none;
            border-radius: 3px;
        }

        button:hover {
            background-color: #c9302c;
        }

        footer {
            margin-top: 20px;
            background-color: #333;
            color: white;
            padding: 10px;
            width: 100%;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <span>Welcome Admin!</span>
        <div class="navbar-links">
            <a href="/logout" class="navbar-link">Logout</a>
        </div>
    </div>

    <h1>Admin Panel - User Management</h1>

    <table>
        <thead>
            <tr>
                <th>User ID</th>
                <th>Username</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% users.forEach(user => { %>
                <tr>
                    <td><%= user._id %></td>
                    <td><%= user.name %></td>
                    <td>
                        <a href="/admin-panel/edit-user/<%= user._id %>">Edit</a>
                        <form action="/admin-panel/delete-user/<%= user._id %>" method="post" onsubmit="return confirm('Are you sure you want to delete this user?');">
                            <button type="submit">Delete</button>
                        </form>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>

    <footer>
        <p style="margin: 0;">Created by Balzhan, SE-2206</p>
    </footer>
</body>
</html>
